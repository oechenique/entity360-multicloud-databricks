# Copiar a .env (ignorado por git). Mínimo 8 caracteres con mayúsculas, minúsculas,
# dígitos y símbolo. Evitar '$' (docker compose lo interpreta). Los dos con el mismo valor.
MSSQL_SA_PASSWORD=<SA_PASSWORD>
SQLCMDPASSWORD=<SA_PASSWORD>
