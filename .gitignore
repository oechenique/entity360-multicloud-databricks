# Terraform
*.tfstate*
.terraform/
*.tfplan
*.tfvars
!*.tfvars.example
crash.log

# Secretos y credenciales
.env
.env.*
!.env.example
profiles.yml
*.pem

# Python
__pycache__/
.venv/
*.pyc

# Referencias (repos viejos, solo lectura)
referencias/

# Datos descargados en el spike (no se versionan, se re-descargan)
spike/data/

# Payloads temporales del spike
spike/consumo/_*.json

# Fase 2: datos descargados del legacy y estado local del extractor CDC
legacy/data/
producers/cdc_extractor/state/
